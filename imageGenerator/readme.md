
# PPM Image generator

Example of usage of the mnktRenderer to implement a PPM image generator.

The program renders a scene using the mnktRenderer and then saves the generated image as a simple PPM file.
